name: Deploy to EC2

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "aws/examples/**"

concurrency:
  group: deploy-ec2
  cancel-in-progress: false

jobs:
  test:
    name: Pre-deploy tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: tests/requirements.txt

      - name: Install dependencies
        run: pip install -r tests/requirements.txt

      - name: Run unit tests
        env:
          PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/item-service:${{ github.workspace }}/notification-service
        run: pytest tests/test_events.py tests/test_duplicate_events.py tests/test_saga.py -v --tb=short

  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Validate secrets
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          if [ -z "$EC2_HOST" ] || [ -z "$EC2_USER" ] || [ -z "$EC2_SSH_KEY" ]; then
            echo "::error::Missing GitHub secrets. Add EC2_HOST, EC2_USER, EC2_SSH_KEY in repo Settings → Secrets."
            exit 1
          fi
          echo "Deploying to $EC2_USER@$EC2_HOST"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure SSH
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$EC2_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$EC2_HOST" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Test SSH connection
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes -o ConnectTimeout=15 \
            "$EC2_USER@$EC2_HOST" "echo SSH OK && docker --version"

      - name: Sync files to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key -o BatchMode=yes" \
            --exclude '.git' \
            --exclude '__pycache__' \
            --exclude '.pytest_cache' \
            --exclude 'frontend/node_modules' \
            --exclude '*.db' \
            --exclude 'aws/dist' \
            ./ "$EC2_USER@$EC2_HOST:~/Lostify/"

      - name: Deploy on EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "$EC2_USER@$EC2_HOST" << 'ENDSSH'
            set -e
            cd ~/Lostify
            chmod +x aws/ec2/*.sh
            # Ensure .env exists with EC2 public IP for reference
            if [ ! -f .env ]; then cp .env.example .env; fi
            PUBLIC_IP=$(curl -sf http://169.254.169.254/latest/meta-data/public-ipv4 || true)
            if [ -n "$PUBLIC_IP" ] && ! grep -q "^PUBLIC_API_URL=" .env; then
              echo "PUBLIC_API_URL=http://$PUBLIC_IP" >> .env
            fi
            docker compose -f docker-compose.prod.yml up --build -d
            sleep 12
          ENDSSH

      - name: Health check
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes "$EC2_USER@$EC2_HOST" \
            "curl -sf http://localhost:3001/api/user/health && curl -sf http://localhost:3001/api/item/health && curl -sf http://localhost:3001/api/notif/health"

      - name: Deployment summary
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
        run: |
          echo "### Deployed to EC2" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend:** http://${EC2_HOST}:3001" >> $GITHUB_STEP_SUMMARY
          echo "- **Health:** http://${EC2_HOST}:3001/api/user/health" >> $GITHUB_STEP_SUMMARY
